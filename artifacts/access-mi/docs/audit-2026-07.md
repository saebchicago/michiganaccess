# Comprehensive Multi-Domain Website Audit - July 2026

**Date:** 2026-07-29
**Scope:** Full-repo audit across architecture, SEO, performance, data
integrity, accessibility, i18n, UX flows, testing/CI, and the
donation/partner funnel. Complements `docs/AUDIT.md` (2026-07-20 route-level
pass); this pass is repo-wide and focused on the gap between the site's
stated value proposition (independent, all 83 counties, every number
traceable) and what actually ships.

**Status legend:** FIXED = addressed in the PR that adds this document.
DOCUMENTED = needs owner decision, credentials, or a larger effort; tracked
here so it is not lost.

---

## 1. Trust and correctness

| ID | Severity | Finding | Evidence | Status |
|----|----------|---------|----------|--------|
| T1 | High | `/energy` and `/legal-aid` exist in `ROUTE_META`, the sitemap, and prerendered HTML, but have no SPA route (removed 2026-07-04 per FIXLOG). Crawlers index soft-404s advertised as canonical 200s. | `src/config/routeMeta.ts:220,229`; `public/sitemap.xml`; no match in `src/config/routes.ts` | FIXED - entries removed, sitemap regenerated |
| T2 | High | No guard asserts `ROUTE_META` paths exist in `APP_ROUTES`; `check-route-reachability.mjs` only checks the reverse direction, so T1 could recur silently. | `scripts/check-route-reachability.mjs` | FIXED - new `scripts/check-routemeta-routes.mjs` wired into build + CI |
| T3 | High | Desktop donation CTA "Support This Project" points to `/about#support`; `AboutPage` has no `id="support"`. The primary support CTA silently scrolls nowhere. | `src/components/layout/Footer.tsx:205`, `src/config/routes.ts:885` | FIXED - links point to `/support` |
| T4 | High | Support page claims donations are "tax-deductible" while the same page says 501(c)(3) status is only "in process", and the orphaned `FounderSupportSection` says "not a registered charity". A false deductibility claim is a legal risk. | `src/pages/SupportPage.tsx:33,115` | FIXED - "tax-deductible" wording removed; honest 501(c)(3) status line kept |
| T5 | Medium | `FounderSupportSection.tsx` is orphaned dead code (imported nowhere) with a second Buy Me a Coffee handle (`accessmichigan` vs the live `michigans`). | `src/components/shared/FounderSupportSection.tsx:59` | FIXED - component deleted; canonical handle remains `buymeacoffee.com/michigans` |
| T6 | High | `src/data/dataFreshness.ts` (rendered on `/methodology` and `/data`) is hand-maintained and contradicts real ingest metadata: lists "PLACES 2024 Release, stale" while the shipped dataset is the 2025 release ingested 2026-07-02; 10 of 15 rows self-labeled stale. | `src/data/dataFreshness.ts` vs `cdc-places-county.generated.json` provenance | FIXED - rows corrected against `.generated.json` provenance. Follow-up idea: a guard that cross-checks this file against generated provenance blocks |
| T7 | Medium | `public/llms.txt` (the AI-crawler surface; robots.txt explicitly welcomes GPTBot/ClaudeBot/PerplexityBot) has 4 dead links (`/community-events`, `/our-intel`, `/partnership`, `/replication`) and 2 mislabels (`/support` labeled "Support Groups"; `/closure-watch` labeled "Site Report"). `check-links.mjs` only scans `src/`. | `public/llms.txt` | FIXED - links corrected; link guard extended to cover `public/llms.txt` |
| T8 | Medium | Contact, Feedback, and Partner inquiry forms submit to Supabase without the `supabaseConfigured` guard used elsewhere; if env vars are unset every partner inquiry fails to a generic toast. `ContactPage` also carries a vestigial `data-netlify="true"` while posting to Supabase. | `src/pages/ContactPage.tsx`, `src/pages/FeedbackPage.tsx`, `src/components/partners/PartnerSubPage.tsx`; correct pattern at `src/pages/ReplicationPage.tsx:382` | FIXED - configured-check + visible failure message added |

## 2. Accessibility and i18n

| ID | Severity | Finding | Evidence | Status |
|----|----------|---------|----------|--------|
| A1 | High | Arabic and Bengali locale files are complete (496 keys each, at parity with English), code-split loading and RTL CSS ship in the bundle, but `LanguageSwitcher` hardcodes only en/es - the translations are unreachable. Dearborn/Hamtramck host the largest Arab-American community in the US; Hamtramck has a large Bengali community. | `src/components/shared/LanguageSwitcher.tsx:13-16`; `src/i18n/locales/{ar,bn}.json` | FIXED - ar/bn enabled with the existing partial-translation badge |
| A2 | High | `document.documentElement.lang`/`dir` are only set on an explicit switcher click. A returning Arabic user gets translated text rendered LTR under `lang="en"` (wrong screen-reader voice, wrong direction) until they reopen the switcher. | `src/i18n/index.ts` never touches `documentElement`; `index.html:2` static `lang="en"` | FIXED - lang/dir applied on boot from the detected language |
| A3 | High | Below 640px, `QuickExitBar` (fixed bottom, `z-[9999]`) and `MobileBottomNav` (fixed bottom, `z-50`) occupy the same edge; the exit bar covers primary mobile navigation. `Layout` reserves padding for only one bar. | `src/components/shared/MobileBottomNav.tsx:32`, `src/components/layout/Layout.tsx:64` | FIXED - nav offset above the exit bar below `sm`; layout padding extended. QuickExitBar untouched (sacrosanct) |
| A4 | Medium | `OnboardingTour` declares `role="dialog" aria-modal="true"` with no focus trap or initial focus; a global `keydown` Enter handler advances the tour while also activating whatever control is focused underneath. | `src/components/shared/OnboardingTour.tsx:131,170-175` | FIXED - initial focus + focus trap added; Enter handler scoped |
| A5 | Medium | ~190 uses of `text-muted-foreground/60`-style opacity modifiers push AA-tuned tokens below 4.5:1; 973 hardcoded palette classes across 106 files bypass tokens entirely, so `HighContrastToggle` has no effect on them; 9-10px text appears at scale (296 uses of `text-[9px]`, 883 of `text-[10px]`). | `src/index.css:37-120,219`; grep counts across `src/` | DOCUMENTED - needs a systematic design pass, not a spot fix |
| A6 | Medium | jsdom axe coverage is 8 of 144 routes and always mocks `Layout`, so the composed document (landmarks, skip-link targets, bars) is never scanned; browser axe specs exist but never run in CI. | `src/test/a11y/*`, `src/test/e2e/accessibility.spec.ts` | PARTIAL - Playwright job added to CI (see C1); coverage expansion documented |
| A7 | Low | `CountySelector` filter input has no label and list has no listbox/option semantics or live result count; `HeroSection` combobox lacks `aria-expanded`/`aria-activedescendant`. | `src/components/shared/CountySelector.tsx:85-92`, `src/components/home/HeroSection.tsx:656` | DOCUMENTED |
| A8 | Low | Forms: `autoComplete` appears once across 152 inputs; `inputMode` 11 times; react-hook-form + zod installed but used by zero forms; several ZIP inputs lack numeric keypad hints. | grep across `src/`; `src/components/ui/form.tsx` unused | DOCUMENTED |
| A9 | Low | `CrisisBar`'s quick-exit uses `window.open` (popup-blockable) + `history.go(-length)` (no-op past history start), weaker than QuickExitBar's `location.replace` pattern. File is sacrosanct; flagged for owner review only. | `src/components/shared/CrisisBar.tsx` | DOCUMENTED - owner decision required |

## 3. UX flows

| ID | Severity | Finding | Evidence | Status |
|----|----------|---------|----------|--------|
| U1 | High | 30 data-fetch call sites never destructure `error`; a failed fetch renders the empty/"no results" branch. On help-finding pages a user cannot distinguish "no clinics near you" from "database unreachable". | `src/components/findcare/ProviderDirectory.tsx:67`, `src/pages/HealthMapPage.tsx:42`, `src/pages/CommunityResourcesPage.tsx:187`, `src/pages/FinancialHelpPage.tsx:65`, +26 more | PARTIAL - fixed at the four help-finding surfaces; remaining 26 documented |
| U2 | Medium | `EmptyState`/`DataUnavailable` shared components exist but are used by ~2 pages; every other zero-result path is bespoke inline text. | `src/components/shared/EmptyState.tsx` | PARTIAL - reused in U1 fixes |
| U3 | Low | Non-functional CTA: "Community insights" button fires `toast.info("coming soon")`; several "coming soon" strings and `TBD` fields (`DataCenterPage` capacity/investment). | `src/pages/ComparePlacesPage.tsx:1475`, `src/pages/DataCenterPage.tsx:77-130` | DOCUMENTED - content decisions |
| U4 | Medium | Three flagship data products run on static fallbacks with `TODO: replace with supabase.functions.invoke` (`useSnapMichigan`, `useSnapCoverageAtRisk`, `useClosureWatch`). | `src/hooks/useSnapMichigan.ts:23` etc. | DOCUMENTED - needs backend decision |
| U5 | Low | Four different "mobile" breakpoints coexist (640/768/1024 across QuickExitBar, useIsMobile, MobileCivicNav, MobileBottomNav); `/domain-dashboard` renders two bottom navs below 768px. | `src/pages/DomainDashboard.tsx:307,466`, `src/components/responsive/ResponsiveGrid.tsx:93` | DOCUMENTED |
| U6 | Low | Print CSS is tuned for ~6 pages while `PrintButton` mounts on all 144; printed resource lists lose URLs (no `a[href]:after` expansion). Caseworker-facing pages (`/resources`, `/find-care`, `/financial-help`) have no print treatment. | `src/index.css:463-509` | DOCUMENTED |

## 4. SEO and social

| ID | Severity | Finding | Evidence | Status |
|----|----------|---------|----------|--------|
| S1 | Medium | Runtime `og:image` is the relative `/opengraph.jpg` (spec requires absolute; Slack/Twitter often drop relative URLs) and differs from the prerendered absolute `og-card.png` - two different social cards. | `src/config/site.ts:3`, `src/hooks/usePageMeta.ts` | FIXED - `DEFAULT_OG_IMAGE` is now the absolute og-card.png |
| S2 | Low | `public/og-image.png` is a 0-byte file referenced nowhere. | `ls -la public/` | FIXED - deleted |
| S3 | Low | 4 of 146 pages never call `usePageMeta` (EmbedWidget, HealthPlansMedicaidPage, UtilitiesRegulatorsPage, CountyRedirect). | grep `usePageMeta` | FIXED for the 3 real pages; CountyRedirect is transient by design |
| S4 | Medium | Sitemap covers 39 static routes + counties; ~104 registered routes have no entry and no prerendered meta because `generate-sitemap.mjs` derives from `ROUTE_META`, not `APP_ROUTES`. | `scripts/generate-sitemap.mjs` | DOCUMENTED - requires ~104 hand-written meta descriptions; content effort |
| S5 | Medium | GA4 is loaded but zero custom instrumentation exists: no SPA route-change `page_view`, no events. Funnel data unreliable for a 140-route SPA. Site-wide `referrer: no-referrer` also strips outbound attribution. | `index.html:4-20`, no `gtag(` in `src/` | FIXED - minimal route-change page_view dispatch added (no new cookies or features) |
| S6 | Low | `humans.txt` says "React 18" (app is React 19.1); `AccessibilityPage` claimed ar/bn + RTL support (false before A1, true after) and print-optimized resource pages (false, see U6). | `public/humans.txt`, `src/pages/AccessibilityPage.tsx:65-70` | FIXED - humans.txt corrected; print claim softened; language claim now true |
| S7 | Medium | Netlify deploy build skips the entire guard chain and both generators (`vite build && prerender-meta` only); deployed sitemap/source-catalog are whatever is committed. CI runs guards separately, so drift between deploy and CI is possible. | `netlify.toml:2`, root `package.json:8` | DOCUMENTED - owner decision (build-time cost vs safety) |
| S8 | Low | `prerender-meta.mjs` parses `routeMeta.ts` with a regex brace-walker; a literal-form change silently drops entries. Documented as intentional; T2's new guard reduces the blast radius. | `scripts/prerender-meta.mjs:62-127` | DOCUMENTED |

## 5. Performance and repo hygiene

| ID | Severity | Finding | Evidence | Status |
|----|----------|---------|----------|--------|
| P1 | Medium | ~2.09 MB of pure runaway leading indentation across 7 page files (Elections.tsx is 101x its content size; single lines up to 6,015 chars). Slows esbuild, typecheck, editors, and agents. | `src/pages/{Elections,ProviderData,PublicSafety,SocialServices,Officials,DisabilityAccess,TransparencyPage}.tsx` | FIXED - whitespace-only reformat, verified with `git diff -w` |
| P2 | Low | Eager homepage chunk bundles ~192 KB raw JSON (`trendSeries.json` + `hrsa-hpsa-county.generated.json`) for two small widgets. | `src/pages/Index.tsx` imports | FIXED - dynamic imports |
| P3 | Low | Dead assets: unreferenced 330 KB `src/assets/hero-michigan.jpg`. | grep | FIXED - deleted |
| P4 | Low | `public/us-counties-10m.json` is a full-US 842 KB TopoJSON for a Michigan-only app; `cdc-places-zcta.generated.json` (2.7 MB) has no live consumer; `mi-zctas.ts` (212 KB) and `zcta-county-crosswalk.ts` (115 KB) have no consumers outside their tests. | `ls -la public/ src/data/` | DOCUMENTED - trimming TopoJSON needs map QA; ZCTA data may be intended for future features |
| P5 | Low | No image pipeline (`<picture>`/AVIF/WebP). Fonts are exemplary (self-hosted, preloaded, subsetted, immutable cache). | `index.html` | DOCUMENTED |

## 6. Data integrity

| ID | Severity | Finding | Evidence | Status |
|----|----------|---------|----------|--------|
| D1 | High | `acs-broadband-county.generated.json` is 100% empty in production: all 83 counties `status: "pending-ci"`, every value null. `build-data.yml` runs `--require-live` weekly which throws without `CENSUS_API_KEY` - if the secret is missing the job fails red every Monday. | `src/data/acs-broadband-county.generated.json` | DOCUMENTED - needs repo-secrets access; check Actions history |
| D2 | Medium | `build-data.yml` is effectively a no-op: both `data-sources.json` entries still carry `_illustrative: true` and are skipped; `public/data/manifest.json` frozen at 2026-06-08. | root `data-sources.json`, `scripts/build-data.mjs` | DOCUMENTED - owner must supply real endpoints |
| D3 | Medium | Facility/trend data last refreshed 2026-06-12 (~7 weeks) despite a weekly workflow; either upstream is static or the workflow is failing. | `verifiedHealthFacilities.json` provenance | DOCUMENTED - check Actions history |
| D4 | Medium | `check-dataset-labels.mjs` filters on `.generated.json`, so `snapCountyGenerated.json` (capital G) and `trendSeries.json` carry no `value_label` despite feeding user-facing figures. | `scripts/check-dataset-labels.mjs` | FIXED - guard tightened (explicit include list); labels added to both datasets |
| D5 | Medium | `verifiedHealthFacilities.json` (423 KB, user-facing) also has no `value_label`, but the file and its ingestion script are sacrosanct (regenerate-only). | `scripts/build-facility-dataset.mjs` | DOCUMENTED - needs a named exception to add the label at ingestion |
| D6 | Low | Provenance chips render on 32 of 146 pages; dynamic `label={expr}` and `source={expr}` are out of guard scope; legacy lowercase `ProvenanceLevel` union coexists with the canonical uppercase one. | `src/types/provenance.ts:1` | DOCUMENTED |
| D7 | Low | `sourceHealth.generated.json` is `{}` and `sourceLinkHealth.generated.json` is null (dev proxy 403s all URLs); the weekly link-check workflow is the real mechanism. | `src/data/source*.generated.json` | DOCUMENTED |

## 7. Testing and CI

| ID | Severity | Finding | Evidence | Status |
|----|----------|---------|----------|--------|
| C1 | Medium | 7 Playwright specs (user journeys, all routes, mobile, browser axe) exist but no CI job runs them. | `src/test/e2e/`, `.github/workflows/ci.yml` | FIXED - non-blocking e2e job added (continue-on-error, promotion note), matching the Lighthouse precedent |
| C2 | Low | `.lighthouserc.cjs` comments still describe the job as continue-on-error; it has been blocking since Round 8. | `.lighthouserc.cjs:7-8,22` | FIXED - comments corrected |
| C3 | Low | `a11y-priority-routes.spec.ts` is designed never to fail (console.log only); `mobile.spec.ts` tap-target assertion is `< 50` small targets (a no-op). | spec files | DOCUMENTED - tighten once e2e job is stable |
| C4 | Low | No coverage thresholds configured; no ESLint config or lint script anywhere. | `vitest.config.ts` | DOCUMENTED |
| C5 | Low | FIXLOG "Phase 0" stack table is stale (React 18, npm, wouter, zustand - none current). | `FIXLOG.md` | DOCUMENTED - FIXLOG edits left to owner convention |

## 8. Positioning and content (owner decisions)

- IA duplication confuses users and crawlers: `/insights` vs `/data-and-insights` vs `/data` vs `/datasets` vs `/data-explorer`; `/civic-data` vs `/civic-data-hub`; `/partners` vs `/partnerships`; `/support` (donate) vs `/support-groups`.
- Positioning tension: the resident product is genuinely free/ad-free and the footer suppresses donation asks on help routes (good), but an enterprise BD funnel (`/bd-financial-model`, `/detection-gap`, `/market-intelligence`) and national-expansion lead capture live on the same domain. Worth a deliberate decision, possibly a separate partner subdomain or clearer separation.
- "Access Michigan Pro" paid tier is pre-announced on `/about` with no timeline.
- Terms effective date is Feb 2025 (~17 months); legal doc, owner should re-date after review.
- Changelog is the designated SSOT of release history but froze at April 2026 while Rounds 5-8 shipped (fixed in this PR through Round 8; keep it current).
- i18n adoption is ~6% of components; enabling ar/bn (A1) raises the value of extracting more strings, starting with the help-finding pages.
- Error tracking is absent (ErrorBoundary logs to console only); consider a privacy-respecting reporter consistent with the site's posture.
